# Games CSV Viewer

Simple static site that reads Games.csv from the repository root and displays it in a table with filters.

Run locally:

```bash
python3 -m http.server 8000
# then open http://localhost:8000
```

Files added: index.html, styles.css, app.js
